import { useCartStore } from '../../store/cartStore';
export function CartDrawer() { const { items, remove, updateQuantity } = useCartStore(); const subtotal = items.reduce((sum, item) => sum + item.unitPrice * item.quantity, 0); return <aside aria-label="Shopping cart" className="card"><h2>Your cart</h2>{items.length === 0 ? <p>Your cart is empty.</p> : items.map((item) => <div key={item.id}><strong>{item.name}</strong><input aria-label={`Quantity for ${item.name}`} type="number" min="1" value={item.quantity} onChange={(e) => updateQuantity(item.id, Number(e.target.value))} /><button onClick={() => remove(item.id)}>Remove</button></div>)}<p>Subtotal: {subtotal.toFixed(2)}</p></aside>; }
