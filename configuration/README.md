# Platform configuration foundation

Configuración no secreta y convenciones comunes para desarrollo local, tests y futuros entornos. Los servicios deben leer variables de entorno; ningún valor sensible se versiona.

Perfiles: `dev`, `test` y `prod`. El archivo `.env.example` es la plantilla local; `configuration/platform.env.example` documenta las variables transversales.
