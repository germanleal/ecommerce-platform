# Provider SPI

Se implementan `AIProvider`, `ChatProvider`, `EmbeddingProvider`, `CompletionProvider`, `VisionProvider` y `AudioProvider`. `ProviderRegistry` inicia vacío; no se registra ningún proveedor concreto.
