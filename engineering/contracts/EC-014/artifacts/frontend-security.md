# Frontend Security

The frontend does not store passwords or raw tokens. API calls use the approved IAM integration boundary and tenant context; errors never expose stack traces or internal configuration.
