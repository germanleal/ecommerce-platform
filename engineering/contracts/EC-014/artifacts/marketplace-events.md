# Marketplace Events

Eventos candidatos, aún no implementados:

- `StoreCreated`.
- `StoreActivated`.
- `ProductCreated`.
- `ProductPublished`.
- `ProductArchived`.

Todos deberán incluir `eventId`, `tenantId`, `aggregateId`, `eventType`, `timestamp`, `correlationId` y `traceId`.
