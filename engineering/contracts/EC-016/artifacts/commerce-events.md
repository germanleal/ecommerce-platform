# Commerce Events

Eventos futuros, no implementados en Parte 1:

- `SellableProductCreatedEvent`.
- `PriceCreatedEvent`.
- `PriceChangedEvent`.
- `ProductAvailabilityChangedEvent`.

Cada evento deberá incluir `eventId`, `tenantId`, `aggregateId`, `eventType`, `occurredAt`, `correlationId` y `traceId`.
