# Integration Events

El consumidor escucha `marketplace.events`, `commerce.events`, `order.events`, `payment.events`, `inventory.events`, `fulfillment.events` y `analytics.events`, y publica el payload recibido en `integration.events` como adapter genérico.
