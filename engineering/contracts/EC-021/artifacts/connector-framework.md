# Connector Framework

`AdapterRegistry` resuelve adapters por provider; `AdapterContext` transporta tenant, connector, correlation y versión. `SynchronizationEngine` ejecuta el adapter sin acoplar el dominio a proveedores.
