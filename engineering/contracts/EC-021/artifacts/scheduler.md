# Scheduler

`IntegrationScheduler` tiene delay configurable y actualiza schedules vencidos. La ejecución manual y scheduled se exponen por API; la selección de jobs multi-tenant requiere principal de sistema en la siguiente fase.
