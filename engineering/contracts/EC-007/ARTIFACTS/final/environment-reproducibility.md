# Environment Reproducibility

La configuración es reproducible por archivos versionados y `.env.example`. La prueba destructiva `docker compose down -v` seguida de `docker compose up` queda pendiente hasta disponer de Docker Desktop Linux engine.
