# KPI Engine

`KpiEngineService` calcula ventas, ticket promedio, ingresos, reembolsos, estados de órdenes, stock, productos y categorías usando únicamente read models tenant-scoped. `trend(period)` soporta día, semana, mes, trimestre y año mediante agrupación temporal PostgreSQL.
