# Export Engine

`ExportService` genera CSV, XLSX empaquetado como Open XML y PDF básico desde el resultado de Report Engine. Las exportaciones reciben el mismo tenant context y filtros temporales que los reportes.
