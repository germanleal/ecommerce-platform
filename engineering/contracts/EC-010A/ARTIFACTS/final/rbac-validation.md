# RBAC Validation — Remediation

The shared converter is implemented in `shared/security`. Existing services still require dependency migration to consume the jar uniformly. Runtime role matrix is therefore not certified.
