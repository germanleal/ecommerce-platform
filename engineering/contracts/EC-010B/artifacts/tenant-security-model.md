# Tenant Security Model

Tenant access requires an authenticated principal, a `UserTenant` relation and the requested permission. `PLATFORM_ADMIN` is the controlled global-access exception. IAM remains owned by EC-010A.
