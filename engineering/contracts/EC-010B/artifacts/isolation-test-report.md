# Isolation Test Report

La estructura de persistencia aplica `tenant_id` en Store y UserTenant, con unicidad de slug por tenant. Las pruebas de aislamiento runtime se implementarán junto con los adaptadores y casos de uso de la siguiente iteración.
