# Metrics Standard

Métricas base: `application.requests.total`, `application.errors.total`, `application.response.time`, `security.authentication.failed`, `security.authorization.denied`, `tenant.context.created`, `tenant.access.denied` y `tenant.operations.total`.
