# Checkout Context

Checkout validates an active Commerce cart, reads current commercial data, creates an immutable Order snapshot, requests cart completion and publishes checkout facts. It never implements payments or duplicates pricing.
