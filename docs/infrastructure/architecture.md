# Infrastructure Architecture

Local First → Containerized → Cloud Ready → Production Evolvable.

Docker Compose is the only local execution mechanism for the MVP. PostgreSQL, Keycloak and Kafka are isolated by responsibility and network.
