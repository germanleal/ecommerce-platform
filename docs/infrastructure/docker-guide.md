# Docker Guide

Copy `.env.example` to `.env`, provide local values, then run `docker compose up -d`. Stop with `docker compose down`; remove persistent data only when an explicit clean reset is intended.
