# Infrastructure Troubleshooting

Use `docker compose ps`, `docker compose logs <service>` and `docker compose config` to inspect the environment. Verify PostgreSQL health before Keycloak and Kafka health before event consumers.
