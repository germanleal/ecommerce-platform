# Application Security Rules

- Validate input at boundaries.
- Return safe, generic errors.
- Never log passwords, complete JWTs, tokens or secrets.
- Use authenticated identity and contextual authorization.
- Keep CORS origins explicit by environment.
