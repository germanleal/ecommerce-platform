package com.company.platform.integration.framework;import org.springframework.stereotype.Component;@Component public class VersionResolver{public String resolve(String requested){return requested==null||requested.isBlank()?"v1":requested;}public boolean compatible(String source,String target){return source==null||target==null||source.equals(target)||source.startsWith("v1")&&target.startsWith("v1");}}
